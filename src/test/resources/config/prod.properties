# Production Environment Configuration
# Base URL for the application under test
base.url=https://www.example.com

# Browser Configuration
browser=chrome
headless=true
browser.window.maximize=true
browser.implicit.wait=10
browser.page.load.timeout=30
browser.script.timeout=30

# WebDriver Configuration
webdriver.chrome.driver=
webdriver.firefox.driver=
webdriver.edge.driver=

# Test Data Configuration
test.data.path=src/test/resources/testdata
test.data.users.file=users.json
test.data.products.file=products.csv

# Database Configuration (if needed)
db.url=jdbc:mysql://prod-db.example.com:3306/testdb
db.username=prod_user
db.password=prod_password
db.driver=com.mysql.cj.jdbc.Driver

# API Configuration
api.base.url=https://api.example.com
api.timeout=30000
api.retry.count=5

# Reporting Configuration
reports.path=target/reports
screenshots.path=target/screenshots
extent.report.name=Production-Test-Report
extent.report.title=Production Environment Test Results

# Logging Configuration
log.level=WARN
log.file.path=logs/prod-automation.log

# Email Configuration (for test reports)
email.enabled=true
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.username=
email.password=
email.recipients=qa-lead@example.com,product-owner@example.com

# Parallel Execution Configuration
parallel.tests=true
thread.count=5

# Wait Configuration
explicit.wait.timeout=20
fluent.wait.timeout=25
fluent.wait.polling=3

# Screenshot Configuration
screenshot.on.failure=true
screenshot.on.success=false
screenshot.format=png

# Video Recording Configuration
video.recording.enabled=true
video.recording.path=target/videos

# Mobile Testing Configuration (if applicable)
mobile.platform=
mobile.device.name=
mobile.app.path=
mobile.automation.name=

# Grid Configuration (if using Selenium Grid)
grid.enabled=true
grid.hub.url=http://selenium-grid.example.com:4444/wd/hub

# Performance Testing Configuration
performance.monitoring.enabled=true
performance.threshold.page.load=2000

# Security Testing Configuration
security.testing.enabled=true

# Accessibility Testing Configuration
accessibility.testing.enabled=true
